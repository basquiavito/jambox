export default function content(props){
    return<>
<section>
    <article>
        <p>{props.contentA}</p>
        <p>{props.contentB}</p>
        <p>{props.contentC}</p>
        <p>{props.contentD}</p>
    </article>
</section>
    </>
}